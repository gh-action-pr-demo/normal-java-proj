name: Dependency Security Check

on:
  pull_request:
    branches:
      - main
      - master
  issue_comment:
    types: [created, edited]
  workflow_dispatch:

# 只在 PR 相关的事件时运行
jobs:
  check-if-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
    outputs:
      is-pr: ${{ steps.check.outputs.is-pr }}
      pr-number: ${{ steps.check.outputs.pr-number }}
      should-trigger: ${{ steps.check.outputs.should-trigger }}
    steps:
      - name: Check if this is a PR and should trigger
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            if (context.eventName === 'pull_request') {
              core.setOutput('is-pr', 'true');
              core.setOutput('pr-number', context.issue.number);
              core.setOutput('should-trigger', 'true');
            } else if (context.eventName === 'issue_comment') {
              // 检查是否是 PR 的评论
              const { data: issue } = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number
              });
              
              if (issue.pull_request) {
                // 获取 PR 信息以获取 head SHA
                const { data: pr } = await github.rest.pulls.get({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: context.issue.number
                });
                
                // 检查评论中是否包含 skip 关键字
                const skipKeywords = [
                  '[skip dependency check]',
                  '[skip-dependency-check]',
                  '[skip dependency-check]',
                  '[skip-dependency check]'
                ];
                
                const commentBody = context.payload.comment.body.toLowerCase();
                const hasSkipKeyword = skipKeywords.some(keyword => 
                  commentBody.includes(keyword.toLowerCase())
                );
                
                if (hasSkipKeyword) {
                  core.setOutput('is-pr', 'true');
                  core.setOutput('pr-number', context.issue.number);
                  core.setOutput('should-trigger', 'true');
                  core.setOutput('pr-head-sha', pr.head.sha);
                  console.log('✅ 检测到跳过检查关键字，将触发 workflow');
                  console.log(`PR head SHA: ${pr.head.sha}`);
                } else {
                  core.setOutput('is-pr', 'false');
                  core.setOutput('should-trigger', 'false');
                  console.log('ℹ️ 评论中未包含跳过检查关键字，跳过 workflow');
                }
              } else {
                core.setOutput('is-pr', 'false');
                core.setOutput('should-trigger', 'false');
              }
            } else {
              core.setOutput('is-pr', 'true'); // workflow_dispatch 也允许运行
              core.setOutput('pr-number', '');
              core.setOutput('should-trigger', 'true');
            }

  dependency-check:
    needs: check-if-pr
    if: needs.check-if-pr.outputs.should-trigger == 'true'
    name: OWASP Dependency Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      pull-requests: write
      actions: read
      issues: write
      statuses: write

    steps:
      - name: Get PR head SHA for issue_comment event
        if: github.event_name == 'issue_comment'
        id: get-pr-sha
        uses: actions/github-script@v7
        with:
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });
            core.setOutput('pr-head-sha', pr.head.sha);
            console.log(`PR head SHA: ${pr.head.sha}`);

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 需要完整历史记录来检查 PR 评论
          ref: ${{ github.event_name == 'issue_comment' && steps.get-pr-sha.outputs.pr-head-sha || github.event.pull_request.head.sha || github.sha }}

      - name: Check if skip file exists
        id: check-skip-file
        run: |
          if [ -f .github/dependency-check-skip ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "✅ 发现跳过检查文件 (.github/dependency-check-skip)，将跳过依赖安全检查"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Check PR comments for skip keyword
        if: steps.check-skip-file.outputs.skip == 'false'
        id: check-skip-comment
        uses: actions/github-script@v7
        with:
          script: |
            const skipKeywords = [
              '[skip dependency check]',
              '[skip-dependency-check]',
              '[skip dependency-check]',
              '[skip-dependency check]'
            ];
            
            let hasSkipKeyword = false;
            
            if (context.eventName === 'issue_comment') {
              // 对于 issue_comment 事件：只检查触发事件的评论
              // 这样每次评论只会影响当前这次触发，后续新的 commit 或评论会重新检查
              const triggerComment = context.payload.comment.body.toLowerCase();
              hasSkipKeyword = skipKeywords.some(keyword => triggerComment.includes(keyword.toLowerCase()));
              
              if (hasSkipKeyword) {
                console.log('✅ 在触发评论中发现跳过检查关键字，将跳过本次依赖安全检查');
                console.log('ℹ️ 注意：此跳过仅对本次触发有效，后续新的 commit 或评论将重新执行检查');
              }
            } else if (context.eventName === 'pull_request') {
              // 对于 pull_request 事件：不检查 skip 评论
              // 因为这是新的 commit 触发的，应该正常执行检查
              // 如果用户想跳过，应该在 commit 后添加评论来触发
              hasSkipKeyword = false;
              console.log('ℹ️ pull_request 事件：正常执行检查，不检查 skip 评论');
            } else {
              // workflow_dispatch 等其他事件：不检查 skip 评论
              hasSkipKeyword = false;
            }
            
            if (hasSkipKeyword) {
              core.setOutput('skip', 'true');
            } else {
              core.setOutput('skip', 'false');
            }

      - name: Comment PR when skipped
        if: (steps.check-skip-file.outputs.skip == 'true' || steps.check-skip-comment.outputs.skip == 'true') && github.event_name != 'workflow_dispatch'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let prNumber = context.issue.number;
            
            let skipReason = '';
            if (fs.existsSync('.github/dependency-check-skip')) {
              skipReason = '配置文件 (`.github/dependency-check-skip`)';
            } else {
              skipReason = 'PR 评论中的跳过关键字';
            }
            
            const comment = `## ⏭️ 依赖安全检查已跳过\n\n` +
              `依赖安全检查已跳过（通过 ${skipReason}）。\n\n` +
              `✅ 状态检查将标记为成功，PR 可以合并。\n\n` +
              `---\n\n` +
              `> 💡 提示：如果后续有新的 commit，将重新执行依赖安全检查。`;
            
            await github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

      - name: Skip check if requested
        if: steps.check-skip-file.outputs.skip == 'true' || steps.check-skip-comment.outputs.skip == 'true'
        run: |
          echo "⏭️ 跳过依赖安全检查（已通过配置文件或 PR 评论请求跳过）"
          echo "✅ 检查已跳过，状态检查将标记为成功"
          exit 0

      - name: Set up JDK 17
        if: steps.check-skip-file.outputs.skip != 'true' && steps.check-skip-comment.outputs.skip != 'true'
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Check for dependency updates
        if: steps.check-skip-file.outputs.skip != 'true' && steps.check-skip-comment.outputs.skip != 'true'
        id: check-updates
        run: |
          echo "检查依赖更新..."
          mvn versions:display-dependency-updates -DoutputFile=target/dependency-updates.txt || true
          if [ -f target/dependency-updates.txt ]; then
            echo "依赖更新信息已保存到 target/dependency-updates.txt"
            # 转换为 Markdown 格式
            if [ -s target/dependency-updates.txt ]; then
              {
                echo "## 依赖更新检查结果"
                echo ""
                echo "### 原始输出"
                echo ""
                echo "\`\`\`"
                cat target/dependency-updates.txt
                echo "\`\`\`"
                echo ""
                echo "### 更新摘要（表格格式）"
                echo ""
                echo "| 依赖坐标 | 当前版本 | 最新版本 |"
                echo "|----------|----------|----------|"
                # 解析 Maven versions 插件输出格式: [INFO]   groupId:artifactId:type:currentVersion -> newVersion
                grep -E "^\[INFO\].*->" target/dependency-updates.txt | \
                  sed 's/\[INFO\]\s*//' | \
                  sed -E 's/^([^:]+:[^:]+:[^:]+):([0-9.]+) -> ([0-9.]+).*/\1 | \2 | \3 |/' | \
                  sed 's/|/|/g' || echo "| 无可用更新 | - | - |"
                echo ""
                echo "> 💡 提示：建议定期更新依赖以获取安全修复和新功能"
              } > target/dependency-updates.md
            fi
          fi
        continue-on-error: true

      - name: Run OWASP Dependency-Check
        if: steps.check-skip-file.outputs.skip != 'true' && steps.check-skip-comment.outputs.skip != 'true'
        id: dependency-check
        run: |
          echo "运行 OWASP Dependency-Check 安全扫描..."
          mvn verify -DskipTests

      - name: Upload dependency check reports
        if: always() && steps.check-skip-file.outputs.skip != 'true' && steps.check-skip-comment.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-reports
          path: |
            target/dependency-check-reports/dependency-check-report.html
            target/dependency-check-reports/dependency-check-report.json
            target/dependency-updates.txt
            target/dependency-updates.md
          retention-days: 30
          if-no-files-found: ignore

      - name: Comment PR with detailed summary
        if: failure() && steps.check-skip-file.outputs.skip != 'true' && steps.check-skip-comment.outputs.skip != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            
            // 获取 PR 编号
            let prNumber = context.issue.number;
            if (context.eventName === 'issue_comment') {
              prNumber = context.issue.number;
            } else if (context.eventName === 'pull_request') {
              prNumber = context.issue.number;
            }
            
            // 获取 Actions 运行链接和 Artifacts 链接
            const runId = context.runId;
            const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${runId}`;
            const artifactsUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${runId}`;
            
            // 获取 Artifacts 信息（用于生成下载链接）
            // 注意：GitHub Artifacts 需要通过 Actions 页面下载，无法直接生成下载链接
            // 但可以提供 Artifacts 页面的链接
            
            let comment = '## 🔒 依赖安全检查失败\n\n';
            comment += '检测到高风险依赖漏洞（CVSS >= 7.0）。\n\n';
            comment += '### 📋 检查结果\n\n';
            comment += '- **状态**: ❌ 失败\n';
            comment += `- **Actions 运行**: [查看详情](${runUrl})\n`;
            comment += `- **报告下载**: [下载 Artifacts（包含 HTML/JSON/TXT 报告）](${artifactsUrl})\n`;
            comment += '  - 在 Actions 运行页面底部找到 "Artifacts" 部分\n';
            comment += '  - 点击 "dependency-check-reports" 下载所有报告\n';
            comment += '  - HTML 报告：`dependency-check-report.html`（推荐，可在浏览器中打开）\n';
            comment += '  - JSON 报告：`dependency-check-report.json`\n';
            comment += '  - 依赖更新：`dependency-updates.txt` 和 `dependency-updates.md`\n\n';
            comment += '---\n\n';
            
            // 读取并解析 JSON 报告（仅用于提取漏洞信息，不显示 JSON）
            let vulnerabilities = [];
            let txtReportContent = '';
            try {
              const reportPath = 'target/dependency-check-reports/dependency-check-report.json';
              if (fs.existsSync(reportPath)) {
                const report = JSON.parse(fs.readFileSync(reportPath, 'utf8'));
                
                if (report.dependencies) {
                  vulnerabilities = report.dependencies
                    .filter(dep => dep.vulnerabilities && dep.vulnerabilities.length > 0)
                    .flatMap(dep => {
                      const depName = dep.packages && dep.packages.length > 0 
                        ? dep.packages[0].name || 'Unknown'
                        : 'Unknown';
                      return dep.vulnerabilities.map(vuln => ({
                        name: depName,
                        cve: vuln.name,
                        severity: vuln.severity || 'Unknown',
                        cvssv3: vuln.cvssv3?.baseScore || vuln.cvssv3?.baseSeverity || vuln.cvssv2?.score || 'N/A',
                        description: vuln.description || '',
                        references: vuln.references || []
                      }));
                    })
                    .sort((a, b) => {
                      const scoreA = parseFloat(a.cvssv3) || 0;
                      const scoreB = parseFloat(b.cvssv3) || 0;
                      return scoreB - scoreA;
                    });
                }
              }
              
              // 读取 HTML 报告并转换为文本格式（用于显示）
              const htmlReportPath = 'target/dependency-check-reports/dependency-check-report.html';
              if (fs.existsSync(htmlReportPath)) {
                const htmlContent = fs.readFileSync(htmlReportPath, 'utf8');
                // 简单提取文本内容（移除 HTML 标签）
                txtReportContent = htmlContent
                  .replace(/<script[^>]*>[\s\S]*?<\/script>/gi, '')
                  .replace(/<style[^>]*>[\s\S]*?<\/style>/gi, '')
                  .replace(/<[^>]+>/g, ' ')
                  .replace(/\s+/g, ' ')
                  .trim();
              }
            } catch (error) {
              console.log('无法解析报告文件:', error.message);
            }
            
            // 添加漏洞列表
            if (vulnerabilities.length > 0) {
              comment += '### 🚨 发现的高风险漏洞\n\n';
              comment += `共发现 **${vulnerabilities.length}** 个高风险漏洞：\n\n`;
              comment += '| 依赖 | CVE | 严重程度 | CVSS 分数 |\n';
              comment += '|------|-----|----------|----------|\n';
              
              const displayCount = Math.min(vulnerabilities.length, 20);
              vulnerabilities.slice(0, displayCount).forEach(v => {
                const name = (v.name || 'Unknown').replace(/\|/g, '\\|').substring(0, 50);
                const cve = (v.cve || 'Unknown').replace(/\|/g, '\\|');
                const severity = (v.severity || 'Unknown').replace(/\|/g, '\\|');
                comment += `| ${name} | ${cve} | ${severity} | ${v.cvssv3} |\n`;
              });
              
              if (vulnerabilities.length > displayCount) {
                comment += `\n*还有 ${vulnerabilities.length - displayCount} 个漏洞，请查看完整报告*\n\n`;
              }
              
              comment += '\n---\n\n';
            }
            
            // 添加修复建议
            comment += '### 💡 修复建议\n\n';
            comment += '1. **更新依赖版本**：优先更新到最新版本（如果可用）\n';
            comment += '2. **查看详细报告**：下载 HTML 报告查看完整信息\n';
            comment += '3. **添加抑制规则**：如果漏洞可以接受，在 `dependency-check-suppression.xml` 中添加抑制规则\n';
            comment += '4. **跳过检查**：如果本次 PR 不涉及依赖变更，可以在 PR 中添加评论 `[skip-dependency-check]` 跳过检查\n';
            comment += '5. **重新提交**：修复后重新提交 PR\n\n';
            comment += '---\n\n';
            
            // 添加原始检查结果（TXT 格式，可折叠）
            if (txtReportContent) {
              comment += '### 📝 原始检查结果（文本格式）\n\n';
              comment += '<details>\n<summary>点击展开查看原始文本报告（前 2000 字符）</summary>\n\n';
              comment += '```\n';
              
              // 限制输出长度
              const maxChars = 2000;
              const displayContent = txtReportContent.length > maxChars 
                ? txtReportContent.substring(0, maxChars) + `\n\n... (还有 ${txtReportContent.length - maxChars} 个字符，请下载完整报告查看)`
                : txtReportContent;
              
              comment += displayContent;
              comment += '\n```\n';
              comment += '</details>\n\n';
            }
            
            // 添加依赖更新信息（如果有，也放到可折叠区域）
            try {
              const updatesPath = 'target/dependency-updates.txt';
              if (fs.existsSync(updatesPath)) {
                const updatesContent = fs.readFileSync(updatesPath, 'utf8');
                if (updatesContent.trim()) {
                  comment += '---\n\n';
                  comment += '### 📦 依赖更新检查结果\n\n';
                  comment += '<details>\n<summary>点击展开查看依赖更新详情</summary>\n\n';
                  
                  // 读取 Markdown 格式的更新报告
                  const updatesMdPath = 'target/dependency-updates.md';
                  if (fs.existsSync(updatesMdPath)) {
                    const mdContent = fs.readFileSync(updatesMdPath, 'utf8');
                    comment += mdContent;
                  } else {
                    comment += '```\n';
                    comment += updatesContent.substring(0, 2000);
                    if (updatesContent.length > 2000) {
                      comment += `\n\n... (还有 ${updatesContent.length - 2000} 个字符)`;
                    }
                    comment += '\n```\n';
                  }
                  
                  comment += '</details>\n';
                }
              }
            } catch (error) {
              console.log('无法读取依赖更新文件:', error.message);
            }
            
            // 发送评论
            await github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

      - name: Report status check result
        if: always()
        env:
          SKIP_FILE: ${{ steps.check-skip-file.outputs.skip }}
          SKIP_COMMENT: ${{ steps.check-skip-comment.outputs.skip }}
          CHECK_OUTCOME: ${{ steps.dependency-check.outcome }}
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            // 检查是否跳过了检查
            const skipFile = process.env.SKIP_FILE === 'true';
            const skipComment = process.env.SKIP_COMMENT === 'true';
            const skipped = skipFile || skipComment;
            
            // 获取 PR 编号和 SHA
            let prNumber = context.issue.number;
            let sha = context.sha;
            
            if (context.eventName === 'issue_comment') {
              prNumber = context.issue.number;
              // 获取 PR 的 head SHA
              const { data: pr } = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber
              });
              sha = pr.head.sha;
            } else if (context.eventName === 'pull_request') {
              sha = context.payload.pull_request.head.sha;
            }
            
            if (skipped) {
              // 跳过检查时，报告成功状态
              await github.rest.repos.createCommitStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                sha: sha,
                state: 'success',
                context: 'Dependency Security Check / OWASP Dependency Check',
                description: '依赖安全检查已跳过（通过配置文件或 PR 评论请求）',
                target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
              });
              console.log('✅ 状态检查已报告为成功（跳过检查）');
            } else {
              // 检查实际的检查结果
              const checkOutcome = process.env.CHECK_OUTCOME || 'unknown';
              
              if (checkOutcome === 'failure') {
                await github.rest.repos.createCommitStatus({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  sha: sha,
                  state: 'failure',
                  context: 'Dependency Security Check / OWASP Dependency Check',
                  description: '发现高风险依赖漏洞',
                  target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
                });
                console.log('❌ 状态检查已报告为失败');
              } else if (checkOutcome === 'success') {
                await github.rest.repos.createCommitStatus({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  sha: sha,
                  state: 'success',
                  context: 'Dependency Security Check / OWASP Dependency Check',
                  description: '依赖安全检查通过',
                  target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
                });
                console.log('✅ 状态检查已报告为成功');
              }
            }

      - name: Check scan result
        if: always()
        env:
          SKIP_FILE: ${{ steps.check-skip-file.outputs.skip }}
          SKIP_COMMENT: ${{ steps.check-skip-comment.outputs.skip }}
          CHECK_OUTCOME: ${{ steps.dependency-check.outcome }}
        run: |
          # 如果跳过了检查，直接成功退出
          if [ "$SKIP_FILE" == "true" ] || [ "$SKIP_COMMENT" == "true" ]; then
            echo "✅ 已跳过依赖安全检查（通过配置文件或 PR 评论请求）"
            echo "状态检查将标记为成功，PR 可以合并"
            exit 0
          fi
          
          # 检查实际的检查结果
          if [ "$CHECK_OUTCOME" == "failure" ]; then
            echo "❌ 发现高风险依赖漏洞，PR 无法合并"
            echo "请修复漏洞后重新提交，或在 PR 中添加评论 [skip-dependency-check] 跳过检查"
            exit 1
          elif [ "$CHECK_OUTCOME" == "success" ]; then
            echo "✅ 依赖安全检查通过"
            exit 0
          elif [ "$CHECK_OUTCOME" == "skipped" ]; then
            echo "⚠️ 依赖检查步骤被跳过"
            exit 0
          else
            echo "⚠️ 检查状态未知: $CHECK_OUTCOME"
            exit 1
          fi
