name: Dependency Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  security-events: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Dependency Review
        id: dep-review
        continue-on-error: true
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high

      - name: Get dependency review results and post comment
        id: get-results
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            
            let commentBody = '';
            let hasVulns = false;
            
            try {
              console.log('开始获取依赖审查信息...');
              console.log(`Head SHA: ${context.payload.pull_request.head.sha}`);
              
              // 方法1: 尝试读取 dependency-review-action 的输出文件
              // dependency-review-action 可能会生成报告文件
              let allVulnerabilities = [];
              let dependencyChanges = [];
              
              // 尝试从多个可能的位置读取报告
              const possibleReportPaths = [
                'dependency-review-results.sarif',
                'dependency-review-summary.md',
                '.github/dependency-review-results.json'
              ];
              
              let reportFound = false;
              for (const reportPath of possibleReportPaths) {
                try {
                  if (fs.existsSync(reportPath)) {
                    console.log(`找到报告文件: ${reportPath}`);
                    const reportContent = fs.readFileSync(reportPath, 'utf8');
                    console.log(`报告文件内容长度: ${reportContent.length}`);
                    // 这里可以根据实际文件格式解析
                    reportFound = true;
                    break;
                  }
                } catch (e) {
                  console.log(`无法读取 ${reportPath}: ${e.message}`);
                }
              }
              
              // 方法2: 使用 GitHub Dependency Graph API 获取漏洞信息
              if (!reportFound) {
                console.log('未找到报告文件，使用 Dependency Graph API 获取漏洞信息...');
                
                try {
                  // 获取 head commit 信息
                  const { data: headCommit } = await github.rest.repos.getCommit({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    ref: context.payload.pull_request.head.sha
                  });
                  
                  console.log(`Head commit 有 ${headCommit.parents.length} 个父 commit`);
                  
                  // 获取 base 到 head 的完整对比，获取所有漏洞
                  const { data: prDiff } = await github.rest.dependencyGraph.diffRange({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    basehead: `${context.payload.pull_request.base.sha}...${context.payload.pull_request.head.sha}`
                  });
                  
                  console.log(`PR diff 返回的漏洞数量: ${(prDiff.vulnerabilities || []).length}`);
                  console.log(`PR diff 返回的变更数量: ${(prDiff.changes || []).length}`);
                  
                  // 获取所有漏洞
                  allVulnerabilities = prDiff.vulnerabilities || [];
                  dependencyChanges = prDiff.changes || [];
                  
                  // 如果 PR diff 没有返回漏洞，尝试获取 head commit 相对于其父 commit 的漏洞
                  if (allVulnerabilities.length === 0 && headCommit.parents && headCommit.parents.length > 0) {
                    console.log('PR diff 未返回漏洞，尝试获取 head commit 的漏洞...');
                    const parentSha = headCommit.parents[0].sha;
                    const { data: headDiff } = await github.rest.dependencyGraph.diffRange({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      basehead: `${parentSha}...${context.payload.pull_request.head.sha}`
                    });
                    
                    allVulnerabilities = headDiff.vulnerabilities || [];
                    dependencyChanges = headDiff.changes || [];
                    console.log(`Head diff 返回的漏洞数量: ${allVulnerabilities.length}`);
                  }
                  
                } catch (apiError) {
                  console.error('Dependency Graph API 调用失败:', apiError);
                  console.error('错误详情:', apiError.message);
                  // 不抛出错误，继续执行，至少显示空报告
                  allVulnerabilities = [];
                  dependencyChanges = [];
                }
              }
              
              console.log(`最终获取到的漏洞数量: ${allVulnerabilities.length}`);
              console.log(`依赖变更数量: ${dependencyChanges.length}`);
              
              // 统计漏洞包（去重）
              const vulnerablePackages = new Set();
              allVulnerabilities.forEach(vuln => {
                vulnerablePackages.add(`${vuln.package.name}@${vuln.package.version}`);
              });

              const hasVulns = allVulnerabilities.length > 0;
              
              // 获取依赖变更和许可证信息
              const changes = dependencyChanges || [];
              
              // 统计许可证问题
              let incompatibleLicenses = 0;
              let invalidSpdxLicenses = 0;
              let unknownLicenses = 0;
              const licenseDetails = [];
              
              changes.forEach(change => {
                if (change.change_type === 'added') {
                  // 检查许可证信息
                  if (change.license) {
                    if (change.license === 'UNKNOWN' || !change.license) {
                      unknownLicenses++;
                      licenseDetails.push({
                        name: change.name,
                        version: change.version,
                        manifest: change.manifest,
                        license: change.license || 'UNKNOWN',
                        type: 'unknown'
                      });
                    }
                  } else {
                    unknownLicenses++;
                    licenseDetails.push({
                      name: change.name,
                      version: change.version,
                      manifest: change.manifest,
                      license: 'UNKNOWN',
                      type: 'unknown'
                    });
                  }
                }
              });

              // 格式化报告
              let commentBody = `## Dependency Review\n\n`;
              commentBody += `**Commit**: \`${context.payload.pull_request.head.sha.substring(0, 7)}\`\n`;
              commentBody += `**检查时间**: ${new Date().toISOString()}\n\n`;
              
              commentBody += `The following issues were found:\n\n`;
              
              // 漏洞统计
              if (vulnerablePackages.size > 0) {
                commentBody += `❌ ${vulnerablePackages.size} vulnerable package(s)\n`;
              } else {
                commentBody += `✅ 0 vulnerable package(s)\n`;
              }
              
              // 许可证统计
              if (incompatibleLicenses > 0) {
                commentBody += `❌ ${incompatibleLicenses} package(s) with incompatible licenses\n`;
              } else {
                commentBody += `✅ 0 package(s) with incompatible licenses\n`;
              }
              
              if (invalidSpdxLicenses > 0) {
                commentBody += `❌ ${invalidSpdxLicenses} package(s) with invalid SPDX license definitions\n`;
              } else {
                commentBody += `✅ 0 package(s) with invalid SPDX license definitions\n`;
              }
              
              if (unknownLicenses > 0) {
                commentBody += `⚠️ ${unknownLicenses} package(s) with unknown licenses.\n`;
              } else {
                commentBody += `✅ 0 package(s) with unknown licenses.\n`;
              }

              // 显示详细漏洞列表
              if (hasVulns) {
                commentBody += `\n### Vulnerable Packages\n\n`;
                
                // 按包分组漏洞
                const vulnByPackage = {};
                allVulnerabilities.forEach(vuln => {
                  const pkgKey = `${vuln.package.name}@${vuln.package.version}`;
                  if (!vulnByPackage[pkgKey]) {
                    vulnByPackage[pkgKey] = {
                      name: vuln.package.name,
                      version: vuln.package.version,
                      ecosystem: vuln.package.ecosystem || 'maven',
                      vulnerabilities: []
                    };
                  }
                  vulnByPackage[pkgKey].vulnerabilities.push(vuln);
                });

                Object.values(vulnByPackage).forEach((pkg, index) => {
                  commentBody += `${index + 1}. **${pkg.name}@${pkg.version}**\n`;
                  commentBody += `   - **Ecosystem**: ${pkg.ecosystem}\n`;
                  commentBody += `   - **Vulnerabilities**: ${pkg.vulnerabilities.length}\n\n`;
                  
                  pkg.vulnerabilities.forEach((vuln, vulnIndex) => {
                    const severityEmoji = vuln.severity === 'critical' ? '🔴' : vuln.severity === 'high' ? '🟠' : vuln.severity === 'moderate' ? '🟡' : '⚪';
                    commentBody += `   ${vulnIndex + 1}. ${severityEmoji} **${vuln.severity || 'unknown'}** severity\n`;
                    
                    if (vuln.advisory) {
                      const advisoryId = vuln.advisory.ghsa_id || vuln.advisory.cve_id || 'N/A';
                      commentBody += `      - **Advisory**: ${advisoryId}\n`;
                      if (vuln.advisory.summary) {
                        commentBody += `      - **Summary**: ${vuln.advisory.summary}\n`;
                      }
                      if (vuln.advisory.cvss?.score) {
                        commentBody += `      - **CVSS Score**: ${vuln.advisory.cvss.score}\n`;
                      }
                    }
                    
                    if (vuln.first_patched_version) {
                      const patchedVersion = vuln.first_patched_version.identifier || vuln.first_patched_version;
                      commentBody += `      - **Patched Version**: ${patchedVersion}\n`;
                    }
                    
                    commentBody += `\n`;
                  });
                });
              }

              // 显示许可证详情
              if (licenseDetails.length > 0) {
                commentBody += `\n### Packages with Unknown Licenses\n\n`;
                licenseDetails.forEach((pkg, index) => {
                  commentBody += `${index + 1}. **${pkg.name}@${pkg.version}**\n`;
                  commentBody += `   - **Manifest**: ${pkg.manifest}\n`;
                  commentBody += `   - **License**: ${pkg.license}\n\n`;
                });
              }

              // 显示依赖变更摘要
              if (changes.length > 0) {
                commentBody += `\n### Dependency Changes\n\n`;
                const added = changes.filter(c => c.change_type === 'added').length;
                const removed = changes.filter(c => c.change_type === 'removed').length;
                const updated = changes.filter(c => c.change_type === 'updated').length;
                
                if (added > 0) commentBody += `➕ Added: ${added}\n`;
                if (removed > 0) commentBody += `➖ Removed: ${removed}\n`;
                if (updated > 0) commentBody += `🔄 Updated: ${updated}\n`;
                
                commentBody += `\n<details>\n<summary>View all changes</summary>\n\n`;
                changes.forEach(change => {
                  const changeTypeEmoji = change.change_type === 'added' ? '➕' : change.change_type === 'removed' ? '➖' : '🔄';
                  commentBody += `${changeTypeEmoji} **${change.manifest}**: ${change.name}@${change.version}\n`;
                });
                commentBody += `\n</details>\n`;
              }

              commentBody += `\n---\n`;
              commentBody += `*此报告由 GitHub Actions 自动生成，每次提交都会更新*\n`;

              // 确保 commentBody 不为空
              if (!commentBody || commentBody.trim().length === 0) {
                commentBody = `## 🔍 依赖安全检查报告\n\n⚠️ **无法生成报告内容**\n\n请查看 GitHub Actions 日志获取更多信息。\n\n---\n*此报告由 GitHub Actions 自动生成*\n`;
              }

              console.log('准备创建新评论...');
              console.log(`评论内容长度: ${commentBody.length} 字符`);
              console.log(`评论内容预览: ${commentBody.substring(0, 200)}...`);

              // 每次都创建新评论，不更新现有评论
              try {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.payload.pull_request.number,
                  body: commentBody
                });
                console.log('新评论创建成功');
              } catch (createError) {
                console.error('创建评论失败:', createError);
                throw createError;
              }

              core.setOutput('has-vulnerabilities', hasVulns ? 'true' : 'false');
              console.log(`设置输出: has-vulnerabilities=${hasVulns}`);
              console.log('脚本执行完成');
            } catch (error) {
              console.error('获取依赖审查信息时出错:', error);
              console.error('错误堆栈:', error.stack);
              
              // 如果 API 调用失败，至少创建一个基本评论
              const errorComment = `## 🔍 依赖安全检查报告\n\n⚠️ **无法获取依赖审查详情**\n\n**错误信息**: \`${error.message}\`\n\n**错误类型**: ${error.name}\n\n请查看 GitHub Actions 日志获取更多信息。\n\n---\n*此报告由 GitHub Actions 自动生成*\n`;
              
              try {
                // 创建新评论（不更新现有评论）
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.payload.pull_request.number,
                  body: errorComment
                });
                console.log('错误评论创建成功');
              } catch (commentError) {
                console.error('创建错误评论时也失败了:', commentError);
                // 如果连评论都创建不了，至少输出到日志
                core.setFailed(`无法创建评论: ${commentError.message}`);
              }
              
              core.setOutput('has-vulnerabilities', 'false');
            }

      - name: Fail if vulnerabilities found
        if: steps.dep-review.outcome == 'failure' || steps.get-results.outputs.has-vulnerabilities == 'true'
        run: |
          echo "发现安全漏洞，请查看 PR 评论中的详细报告"
          exit 1
