name: Dependency Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven (to generate dependency graph)
        run: |
          mvn clean compile -DskipTests || echo "构建失败，但继续检查依赖"
          echo "Maven 构建完成，依赖图已生成"

      - name: Dependency Review
        id: dep-review
        continue-on-error: true
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
          deny-licenses: []
          deny-ghsas: []
          # 显示所有漏洞，不仅仅是新引入的
          comment-summary-in-pr: always
          # 允许所有许可证（只检查安全漏洞）
          allow-licenses: []
          # 允许所有 GHSA（只检查漏洞，不阻止）
          allow-ghsas: []

      - name: Debug - Check dependencies
        run: |
          echo "=== 检查 pom.xml 文件 ==="
          find . -name "pom.xml" -type f
          echo ""
          echo "=== module-service/pom.xml 内容 ==="
          cat module-service/pom.xml 2>/dev/null || echo "文件不存在"
          echo ""
          echo "=== 检查 log4j 依赖 ==="
          grep -r "log4j" --include="*.xml" . || echo "未找到 log4j"
          echo ""
          echo "=== Dependency Review 输出状态 ==="
          echo "Outcome: ${{ steps.dep-review.outcome }}"
          echo "Exit code: ${{ steps.dep-review.exit-code }}"

      - name: Get dependency review results and post comment
        id: get-results
        # 无论是否失败都执行，以便显示所有漏洞信息
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            try {
              const depReviewOutcome = '${{ steps.dep-review.outcome }}';
              const hasVulnerabilities = depReviewOutcome === 'failure';
              
              console.log(`Dependency Review 结果: ${depReviewOutcome}`);
              console.log(`是否检测到漏洞: ${hasVulnerabilities}`);
              console.log(`Head SHA: ${context.payload.pull_request.head.sha}`);
              console.log(`Base SHA: ${context.payload.pull_request.base.sha}`);
              console.log(`Run ID: ${context.runId}`);
              
              // 如果没有检测到漏洞，也创建一个评论说明
              if (!hasVulnerabilities) {
                const summaryUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
                let noVulnComment = `## 🔒 Dependency Review\n\n`;
                noVulnComment += `✅ **未检测到高严重程度的安全漏洞**\n\n`;
                noVulnComment += `**Commit**: \`${context.payload.pull_request.head.sha.substring(0, 7)}\`\n\n`;
                noVulnComment += `<a href="${summaryUrl}" target="_blank" rel="noopener noreferrer">📊 查看完整的依赖安全检查报告</a>\n\n`;
                noVulnComment += `**注意**: 如果预期应该检测到漏洞（如 log4j 2.13.0），请检查：\n`;
                noVulnComment += `1. 依赖是否正确添加到 pom.xml\n`;
                noVulnComment += `2. GitHub Dependency Graph 是否已更新\n`;
                noVulnComment += `3. 查看 Actions summary 页面的详细日志\n\n`;
                noVulnComment += `---\n`;
                noVulnComment += `*此评论由 GitHub Actions 自动生成*\n`;
                
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.payload.pull_request.number,
                  body: noVulnComment
                });
                console.log('已创建"未检测到漏洞"的评论');
                return;
              }
              
              console.log('检测到漏洞，开始生成报告...');
              
              // 使用 Dependency Graph API 获取漏洞详情
              let vulnerabilities = [];
              try {
                console.log('尝试获取 base 分支的依赖信息...');
                const { data: baseDiff } = await github.rest.dependencyGraph.diffRange({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  basehead: `${context.payload.pull_request.base.sha}^...${context.payload.pull_request.base.sha}`
                });
                console.log(`Base 分支漏洞数: ${baseDiff.vulnerabilities?.length || 0}`);
                
                console.log('尝试获取 head 分支的依赖信息...');
                const { data: headDiff } = await github.rest.dependencyGraph.diffRange({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  basehead: `${context.payload.pull_request.head.sha}^...${context.payload.pull_request.head.sha}`
                });
                console.log(`Head 分支漏洞数: ${headDiff.vulnerabilities?.length || 0}`);
                
                // 获取 PR diff 中的漏洞（新引入的）
                const { data: prDiff } = await github.rest.dependencyGraph.diffRange({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  basehead: `${context.payload.pull_request.base.sha}...${context.payload.pull_request.head.sha}`
                });
                console.log(`PR diff 漏洞数: ${prDiff.vulnerabilities?.length || 0}`);
                
                // 优先使用 head 分支的所有漏洞（包括已存在的）
                if (headDiff.vulnerabilities && headDiff.vulnerabilities.length > 0) {
                  vulnerabilities = headDiff.vulnerabilities;
                  console.log(`使用 head 分支的漏洞: ${vulnerabilities.length} 个`);
                } else if (prDiff.vulnerabilities && prDiff.vulnerabilities.length > 0) {
                  vulnerabilities = prDiff.vulnerabilities;
                  console.log(`使用 PR diff 的漏洞: ${vulnerabilities.length} 个`);
                }
              } catch (apiError) {
                console.log('Dependency Graph API 调用失败:', apiError.message);
                console.log('错误详情:', JSON.stringify(apiError, null, 2));
                // 继续执行，即使无法获取详细漏洞信息
              }
              
              // 构建 Actions summary 页面链接
              const summaryUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
              
              // 创建评论内容
              let commentBody = `## 🔒 Dependency Review\n\n`;
              commentBody += `⚠️ **检测到安全漏洞**\n\n`;
              commentBody += `**Commit**: \`${context.payload.pull_request.head.sha.substring(0, 7)}\`\n\n`;
              
              // 如果有详细的漏洞信息，显示版本升级建议
              if (vulnerabilities.length > 0) {
                commentBody += `### 📋 漏洞详情及修复建议\n\n`;
                
                // 按包分组
                const vulnByPackage = {};
                vulnerabilities.forEach(vuln => {
                  const pkgName = vuln.package?.name || 'unknown';
                  const pkgVersion = vuln.package?.version || 'unknown';
                  const pkgKey = `${pkgName}@${pkgVersion}`;
                  
                  if (!vulnByPackage[pkgKey]) {
                    vulnByPackage[pkgKey] = {
                      name: pkgName,
                      version: pkgVersion,
                      ecosystem: vuln.package?.ecosystem || 'maven',
                      vulnerabilities: []
                    };
                  }
                  
                  vulnByPackage[pkgKey].vulnerabilities.push({
                    severity: vuln.severity || 'unknown',
                    ghsa_id: vuln.advisory?.ghsa_id,
                    cve_id: vuln.advisory?.cve_id,
                    summary: vuln.advisory?.summary,
                    patched_version: vuln.first_patched_version?.identifier || vuln.first_patched_version
                  });
                });
                
                // 生成漏洞列表，显示当前版本和目标版本
                Object.values(vulnByPackage).forEach((pkg, index) => {
                  const patchedVersions = [...new Set(pkg.vulnerabilities.map(v => v.patched_version).filter(Boolean))];
                  const targetVersion = patchedVersions.length > 0 ? patchedVersions[0] : '待确定';
                  
                  commentBody += `${index + 1}. **${pkg.name}**\n`;
                  commentBody += `   - 📦 **当前版本**: \`${pkg.version}\`\n`;
                  commentBody += `   - ✅ **建议升级至**: \`${targetVersion}\`\n`;
                  
                  pkg.vulnerabilities.forEach((vuln, vulnIndex) => {
                    const severityEmoji = vuln.severity === 'critical' ? '🔴' : vuln.severity === 'high' ? '🟠' : vuln.severity === 'moderate' ? '🟡' : '⚪';
                    commentBody += `   - ${severityEmoji} **${vuln.severity || 'unknown'}** severity`;
                    if (vuln.ghsa_id || vuln.cve_id) {
                      commentBody += ` (${vuln.ghsa_id || vuln.cve_id})`;
                    }
                    commentBody += `\n`;
                  });
                  commentBody += `\n`;
                });
              }
              
              commentBody += `<a href="${summaryUrl}" target="_blank" rel="noopener noreferrer">📊 查看完整的依赖安全检查报告</a>\n\n`;
              
              // 添加 @dependabot 命令说明
              commentBody += `### 🤖 可用命令\n\n`;
              commentBody += `你可以在 PR 评论中使用以下命令：\n\n`;
              commentBody += `- \`@dependabot rebase\` - 重新基于最新代码\n`;
              commentBody += `- \`@dependabot recreate\` - 重新创建此 PR\n`;
              commentBody += `- \`@dependabot merge\` - 合并此 PR\n`;
              commentBody += `- \`@dependabot close\` - 关闭此 PR\n`;
              commentBody += `- \`@dependabot ignore this dependency\` - 忽略此依赖的更新\n\n`;
              
              commentBody += `**提示**: Dependabot 会自动创建 PR 来修复安全漏洞。\n\n`;
              commentBody += `---\n`;
              commentBody += `*此评论由 GitHub Actions 自动生成*\n`;

              console.log('准备创建新评论...');
              console.log(`评论内容长度: ${commentBody.length} 字符`);
              console.log(`Summary URL: ${summaryUrl}`);
              console.log(`评论内容预览: ${commentBody.substring(0, 200)}...`);

              // 每次都创建新评论，不更新现有评论
              try {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.payload.pull_request.number,
                  body: commentBody
                });
                console.log('新评论创建成功');
              } catch (createError) {
                console.error('创建评论失败:', createError);
                throw createError;
              }

              // 由于检测到漏洞，设置输出
              core.setOutput('has-vulnerabilities', 'true');
              console.log('脚本执行完成');
            } catch (error) {
              console.error('生成报告时出错:', error);
              console.error('错误堆栈:', error.stack);
              
              // 构建 Actions summary 页面链接
              const summaryUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
              
              // 即使出错，也创建一个包含 summary 链接的评论（在新标签页打开）
              let errorComment = `## 🔒 Dependency Review\n\n⚠️ **检测到安全漏洞**\n\n`;
              errorComment += `**Commit**: \`${context.payload.pull_request.head.sha.substring(0, 7)}\`\n\n`;
              errorComment += `<a href="${summaryUrl}" target="_blank" rel="noopener noreferrer">📊 查看完整的依赖安全检查报告（包含修复版本建议）</a>\n\n`;
              errorComment += `**注意**: 生成评论时遇到错误: \`${error.message}\`\n\n`;
              errorComment += `**提示**: 请查看 Actions summary 页面获取详细的漏洞信息和修复版本建议。\n\n`;
              errorComment += `---\n`;
              errorComment += `*此评论由 GitHub Actions 自动生成*\n`;
              
              try {
                // 创建新评论（不更新现有评论）
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.payload.pull_request.number,
                  body: errorComment
                });
                console.log('错误评论创建成功');
              } catch (commentError) {
                console.error('创建错误评论时也失败了:', commentError);
                // 如果连评论都创建不了，至少输出到日志
                core.setFailed(`无法创建评论: ${commentError.message}`);
              }
              
              core.setOutput('has-vulnerabilities', 'true');
            }

      - name: Fail if vulnerabilities found
        if: steps.dep-review.outcome == 'failure'
        run: |
          echo "发现安全漏洞，请查看 PR 评论中的详细报告"
          exit 1
